# Setup Grounded-SAM (submodule)
git submodule update --init --recursive

# Install dependencies
To install Grounded SAM check: https://github.com/IDEA-Research/Grounded-SAM-2?tab=readme-ov-file 

## Labeling

- **single_label.py**  
  Demo mode for a single image — just provide the image file path.

- **batch_label.py**  
  Runs labeling on multiple images and compares predictions with ground-truth labels.
